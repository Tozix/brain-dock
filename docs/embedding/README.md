# Embedding

Локальные эмбеддинги через Ollama за единым интерфейсом `EmbeddingProvider`
(по умолчанию `nomic-embed-text`; поддержка bge/mxbai/snowflake). Batch, кэш,
версионирование модели/размерности. Правила — [Claude.md](../../Claude.md) §14.

_Заполняется в Phase 3._
